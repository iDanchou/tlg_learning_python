#! /usr/bin/python3
hostname = input('What value should we set for the hostname: ')

if hostname.lower() == 'mtg': # if conditions are met, these commands will run
    print('The hostname was found to be mtg')
    print('hostname matches expected config')

print('Exiting the script') # indication of the closure of the script
